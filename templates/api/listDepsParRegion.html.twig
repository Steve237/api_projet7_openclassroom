{% extends 'base.html.twig' %}

{% block title %}Hello ApiController!{% endblock %}

{% block body %}

<div class="container">
    <form action"" method="GET">
        <div class="row">
            <div class="col-md-10">
                <div class="form-group">
                    <select class="form-controls" name="region" id="region">
                        <option value="Toutes">
                            Toutes les régions
                        </option>
                        {% for maRegion in mesRegions %}
                            <option value="{{maRegion.code}}" {% if app.request.query.get("region") == maRegion.code %} selected {% endif %}>{{maRegion.nom}}</option>
                        {% endfor %}
                    </select>
                </div>
            </div>
            <div class="col-md-2">
                <button type="submit" class="btn btn-primary">Rechercher</button>

            </div>


        </div>
    </form>
    
    <div class="row">
        {% for monDep in mesDeps %}
        <div class="card text-white bg-primary mb-3" style="max-width: 20rem;">
            <div class="card-header">{{monDep.code}}</div>
            <div class="card-body">
              <h4 class="card-title">{{monDep.nom}}</h4>
              <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
            </div>
        </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
